apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servicegateway.fullname" . }}-test-connection"
  labels:
{{ include "servicegateway.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "servicegateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
